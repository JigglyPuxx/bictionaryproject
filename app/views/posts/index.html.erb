<h1>BICtionary</h1>
<% @posts.each do |post| %>
	<div class="well">
		<h3><%= post.title %></h3>
		<p><%= post.body %></p>	
		<% if user_signed_in? %>
		<%= link_to "Read More", post_path(post), :class => 'btn btn-default' %>
		<% end %>
	</div>
<% end %>